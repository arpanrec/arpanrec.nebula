---
- name: Golang | Molecule | Verify
  hosts: all
  gather_facts: true
  tasks:
      - name: Golang | Molecule | Verify | Debug
        ansible.builtin.debug:
            msg: "{{ lookup('arpanrec.nebula.version_db', 'bws', app_name11='app_name_231532562375237') }}"

      - name: Golang | Molecule | Verify | Debug
        ansible.builtin.debug:
            msg: "{{ version_details.download_link }}"
        vars:
            bws_sdk_version_tag: "1.0.0"
            version_details: "{{ lookup('arpanrec.nebula.version_db', 'bws', github_api_url='https://api.github.com',
                bws_sdk_version_tag=bws_sdk_version_tag) }}"
