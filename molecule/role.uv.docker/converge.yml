---
- name: Uv | Molecule | Converge
  hosts: all
  gather_facts: false
  any_errors_fatal: true
  become: false
  tasks:
      - name: Uv | Molecule | Converge | Include Uv Role
        ansible.builtin.import_role:
            name: arpanrec.nebula.uv
