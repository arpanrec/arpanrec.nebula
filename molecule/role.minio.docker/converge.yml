---
- name: Minio | Molecule | Converge
  hosts: all
  become: false
  gather_facts: false
  any_errors_fatal: false
  tasks:
      - name: Minio | Molecule | Converge | Role
        become: true
        become_user: root
        become_method: ansible.builtin.su
        ansible.builtin.import_role:
            name: arpanrec.nebula.minio
